# As an exercise, use incremental development to write a function called hypotenuse that returns the length of the
# # hypotenuse of a right triangle given the lengths of two legs as arguments. Record each stage of the incremental
# # development process as you go.

import math


def hypotenuse(perpendicular=5, base=5):
    perpendicular
    base
    result = math.sqrt(perpendicular ** 2 + base ** 2)
    return print(result)


hypotenuse()
